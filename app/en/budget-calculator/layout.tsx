// /app/en/budget-calculator/layout.tsx
import metadata from "./metadata";
export { metadata };

export default function BudgetCalculatorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
